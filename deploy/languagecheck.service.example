[Unit]
Description=LanguageCheck font coverage web app
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/languagecheck-server
Environment=HOST=127.0.0.1
Environment=PORT=3000
ExecStart=/usr/bin/npm start
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
